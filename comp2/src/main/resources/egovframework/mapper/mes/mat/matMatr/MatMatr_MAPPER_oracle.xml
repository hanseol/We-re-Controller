<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mes.mat.matr.service.impl.MaterialMapper">
	
	<!-- 자재재고조회페이지 전체 리스트 출력 -->
	<select id="selectMaterialList" parameterType="mes.mat.matr.service.MaterialVO" resultType="egovMap">
		SELECT *
		FROM   (
		              SELECT a.*,
		                     ROWNUM rnum
		              FROM   (
		                              SELECT   c.com_material_code,
		                                       c.com_material_name,
		                                       c.com_material_size,
		                                       c.com_material_unit,
		                                       c.com_material_vendor_code,
		                                       (m.mat_inout_quantity - CASE m.mat_inout_gubun
										                                WHEN 'INOUT003'
										                                THEN mat_inout_quantity
										                                ELSE 0
										                                END						) as material_stock,
		                                       c.com_material_safety_stock,
		                                       c.com_material_min,
		                                       c.com_material_max
		                              FROM     com_material c,
		                                       mat_inout m,
		                                       erp_material_order e
		                              WHERE    c.com_material_code = m.com_material_code
		                              AND	   c.com_material_vendor_code = e.erp_vendor_code
		                              <if test="erpVendorCode != null and erpVendorCode != ''">
		                                AND
							        	q.erp_vendor_code = #{erpVendorCode}
		                              </if>
		                              <if test="comMaterialCode != null and comMaterialCode != ''">
		                              	AND
							        	q.com_material_code = #{comMaterialCode}
		                              </if>
		                              ORDER BY com_material_code DESC
		                              <![CDATA[ ) a
		              WHERE  ROWNUM <= #{lastIndex} )
		WHERE  rnum >= #{firstIndex}
		]]>
	</select>
</mapper>
