<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mes.mat.order.service.impl.MatOrderMapper">
	
	<!-- 자재발주조회페이지 리스트 출력(검사완료한 목록 제외) -->
	<select id="selectMatOrderList" parameterType="mes.mat.order.service.MatOrderVO" resultType="egovMap">
		SELECT *
		FROM   (
		              SELECT a.*,
		                     ROWNUM rnum
		              FROM   (
		                              SELECT   TO_CHAR(e.erp_material_order_date,'YYYY-MM-DD') erp_material_order_date,
		                                       e.erp_material_order_code,
		                                       e.erp_vendor_code,
		                                       get_code_detail(e.erp_vendor_code) as vendor_name, 
		                                       e.com_material_code,
		                                       c.com_material_name,
		                                       c.com_material_size,
		                                       c.com_material_unit,
		                                       e.erp_material_order_qty,
		                                       e.erp_material_unit_price,
		                                       e.erp_material_order_qty * e.erp_material_unit_price as erp_material_price
		                                       
		                              FROM     erp_material_order e,
		                                       com_material c
		                                       
		                              WHERE    e.com_material_code = c.com_material_code
		                              AND      e.erp_material_order_code NOT IN (SELECT	erp_material_order_code
	                                                                             FROM	qua_material_chk
	                                                                             WHERE  qua_material_chk = '1')
		                              <if test="comMaterialCode != null and comMaterialCode != ''">
		                              	AND
							        	e.com_material_code LIKE '%' || #{comMaterialCode} || '%'
		                              </if>
		                              <if test="erpMaterialOrderDate != null and erpMaterialOrderDate != ''">
		                              	AND
							        	e.erp_material_order_date BETWEEN #{erpMaterialOrderDate} AND #{erpMaterialOrderEndDate}
		                              </if>
		                              <if test="erpMaterialOrderCode != null and erpMaterialOrderCode != ''">
		                              	AND
							        	e.erp_material_order_code LIKE '%' || #{erpMaterialOrderCode} || '%'
		                              </if>
		                              ORDER BY e.erp_material_order_code <![CDATA[ ) a
		              WHERE  ROWNUM <= #{lastIndex} )
		WHERE  rnum >= #{firstIndex}
		]]>
	</select>
	
	<!-- 자재입고검사관리 더블클릭 모달(더미생산용)-->
	<select id="searchMatOrderCodeList" parameterType="mes.mat.order.service.MatOrderVO" resultType="egovMap">
		SELECT *
		FROM   (
		              SELECT a.*,
		                     ROWNUM rnum
		              FROM   (
		                              SELECT   e.erp_material_order_code,
		                                       e.com_material_code,
		                                       c.com_material_name,
		                                       c.com_material_size,
		                                       c.com_material_unit,
		                                       e.erp_material_order_qty,
		                                       e.erp_vendor_code,
		                                       e.erp_material_unit_price,
		                                       d.com_code_detail_name,
		                                       TO_CHAR(e.erp_material_order_date, 'YYYY-MM-DD')as erp_material_order_date,
		                                       TO_CHAR(e.erp_material_request_date, 'YYYY-MM-DD')as erp_material_request_date
		                              FROM     erp_material_order e,
		                              		   com_material c,
		                              		   com_code_detail d
		                              WHERE	   e.com_material_code = c.com_material_code
		                              AND	   e.erp_material_order_code NOT IN (SELECT	erp_material_order_code
		                              											 FROM	qua_material_chk)
		                              AND	   e.erp_vendor_code = d.com_code_detail_id
					                  <if test="erpMaterialOrderCode != null and erpMaterialOrderCode !=''">AND
					                     e.erp_material_order_code LIKE '%' || UPPER(#{erpMaterialOrderCode}) || '%'
					                  </if>
					                  <if test="comMaterialName != null and comMaterialName !=''">AND
					                     c.com_material_name LIKE '%' || #{comMaterialName} || '%'
					                  </if>
                  					  ORDER BY e.erp_material_order_code <![CDATA[ ) A
					  WHERE ROWNUM <= #{lastIndex} )
		WHERE RNUM >= #{firstIndex}]]>
	</select>
	
	
	<!-- 자재발주조회페이지 발주코드 모달(검사완료한 목록 제외) -->
	<select id="searchMatOrderCodePureList" parameterType="mes.mat.order.service.MatOrderVO" resultType="egovMap">
		SELECT *
		FROM   (
		              SELECT a.*,
		                     ROWNUM rnum
		              FROM   (
		                              SELECT   e.erp_material_order_code,
		                                       e.com_material_code,
		                                       c.com_material_name,
		                                       c.com_material_size,
		                                       c.com_material_unit,
		                                       e.erp_material_order_qty,
		                                       e.erp_vendor_code,
		                                       e.erp_material_unit_price,
		                                       d.com_code_detail_name,
		                                       TO_CHAR(e.erp_material_order_date, 'YYYY-MM-DD')as erp_material_order_date,
		                                       TO_CHAR(e.erp_material_request_date, 'YYYY-MM-DD')as erp_material_request_date
		                              FROM     erp_material_order e,
		                              		   com_material c,
		                              		   com_code_detail d
		                              WHERE	   e.com_material_code = c.com_material_code
		                              AND	   e.erp_vendor_code = d.com_code_detail_id
  		                              AND      e.erp_material_order_code NOT IN (SELECT	erp_material_order_code
								                                                 FROM	qua_material_chk
								                                                 WHERE  qua_material_chk = '1')
					                  <if test="erpMaterialOrderCode != null and erpMaterialOrderCode !=''">AND
					                     e.erp_material_order_code LIKE '%' || UPPER(#{erpMaterialOrderCode}) || '%'
					                  </if>
					                  <if test="comMaterialName != null and comMaterialName !=''">AND
					                     c.com_material_name LIKE '%' || #{comMaterialName} || '%'
					                  </if>
                  					  ORDER BY e.erp_material_order_code <![CDATA[ ) A
					  WHERE ROWNUM <= #{lastIndex} )
		WHERE RNUM >= #{firstIndex}]]>
	</select>
	
	
	<!-- 불량이 있는 발주코드 모달 -->
	<select id="searchMatOrderCodeFltyList" parameterType="mes.mat.order.service.MatOrderVO" resultType="egovMap">
		SELECT *
		FROM   (
		              SELECT a.*,
		                     ROWNUM rnum
		              FROM   (
		                              SELECT   e.erp_material_order_code,
		                                       e.com_material_code,
		                                       c.com_material_name,
		                                       c.com_material_size,
		                                       c.com_material_unit,
		                                       e.erp_material_order_qty,
		                                       e.erp_vendor_code,
		                                       e.erp_material_unit_price,
		                                       d.com_code_detail_name,
		                                       TO_CHAR(e.erp_material_order_date, 'YYYY-MM-DD')as erp_material_order_date,
		                                       TO_CHAR(e.erp_material_request_date, 'YYYY-MM-DD')as erp_material_request_date
		                              FROM     erp_material_order e,
		                              		   com_material c,
		                              		   com_code_detail d,
                                         	   qua_material_f f
		                              WHERE	   e.com_material_code = c.com_material_code
		                              AND	   e.erp_vendor_code = d.com_code_detail_id
	                                  AND      e.erp_material_order_code = f.erp_material_order_code
	                                  AND      f.qua_material_f_qty != 0
					                  <if test="erpMaterialOrderCode != null and erpMaterialOrderCode !=''">AND
					                     e.erp_material_order_code LIKE '%' || UPPER(#{erpMaterialOrderCode}) || '%'
					                  </if>
					                  <if test="comMaterialName != null and comMaterialName !=''">AND
					                     c.com_material_name LIKE '%' || #{comMaterialName} || '%'
					                  </if>
                  					  ORDER BY e.erp_material_order_code <![CDATA[ ) A
					  WHERE ROWNUM <= #{lastIndex} )
		WHERE RNUM >= #{firstIndex}]]>
	</select>
	

	
</mapper>
