<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mes.mat.match.service.impl.MatMatchMapper">

	

	<select id="selectMatMatchList" parameterType="mes.mat.match.service.MatMatchVO" resultType="egovMap">
		SELECT *
		FROM   (
		              SELECT a.*,
		                     ROWNUM rnum
		              FROM   (
		                              SELECT   mat_match_statement ,
		                                       com_material_code ,
		                                       mat_lot_no ,
		                                       mat_match_inout ,
		                                       mat_match_qty ,
		                                       TO_CHAR(mat_match_date,'YYYY/MM/DD') mat_match_date
		                              FROM     mat_match
		                              WHERE    1=1 <if test="searchKeyword != null and searchKeyword != ''"> <if test="searchCondition == 0">
		                              AND      mat_match_statement = #{searchKeyword} </if> <if test="searchCondition == 1">
		                              AND      com_material_code LIKE '%'
		                                                || #{searchKeyword}
		                                                || '%' </if> </if>
		                              ORDER BY mat_match_statement DESC
		                              <![CDATA[ ) a
		              WHERE  ROWNUM <= #{lastIndex} )
		WHERE  rnum > #{firstIndex}
		]]>
	</select>
	
	<select id="selectMatMatchListTotCnt" parameterType="mes.mat.match.service.MatMatchVO" resultType="int">
			SELECT COUNT(*) totcnt
			FROM MAT_MATCH
			WHERE 1=1
			<if test="searchKeyword != null and searchKeyword != ''">
				<if test="searchCondition == 0">AND
					MAT_MATCH_STATEMENT = #{searchKeyword}
				</if>
				<if test="searchCondition == 1">AND
					COM_MATERIAL_CODE LIKE '%' || #{searchKeyword} || '%'
				</if>
			</if>
	</select>

</mapper>
